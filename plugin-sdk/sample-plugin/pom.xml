<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.example</groupId>
  <artifactId>shipflow-sample-plugin</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>jar</packaging>
  <name>ShipFlow Sample Plugin</name>

  <properties>
    <java.version>21</java.version>
    <maven.compiler.source>21</maven.compiler.source>
    <maven.compiler.target>21</maven.compiler.target>
    <!-- Matches plugin-sdk/shipflow-plugin-api's own artifact version (independent of the
         ShipFlow app/milestone version — see that module's pom.xml). -->
    <shipflow-plugin-api.version>1.0.0</shipflow-plugin-api.version>
    <spring-boot.version>3.4.0</spring-boot.version>
    <!-- Matches the backend's pinned Lombok version (backend/pom.xml) — older Lombok releases
         don't support newer JDKs' internal javac APIs (annotation processing fails with a
         "TypeTag :: UNKNOWN" ExceptionInInitializerError). -->
    <lombok.version>1.18.46</lombok.version>
  </properties>

  <dependencies>
    <!-- ShipFlow Plugin API — provided at runtime by the host application.
         Real, independently-buildable artifact: plugin-sdk/shipflow-plugin-api
         (`mvn install` it first so it resolves from your local repo). -->
    <dependency>
      <groupId>com.github.farzadsedaghatbin.shipflow</groupId>
      <artifactId>shipflow-plugin-api</artifactId>
      <version>${shipflow-plugin-api.version}</version>
      <scope>provided</scope>
    </dependency>

    <!-- Spring annotations — provided by the host Spring Boot app -->
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-context</artifactId>
      <version>6.1.0</version>
      <scope>provided</scope>
    </dependency>

    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <version>${lombok.version}</version>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
        <configuration>
          <release>21</release>
          <annotationProcessorPaths>
            <path>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
              <version>${lombok.version}</version>
            </path>
          </annotationProcessorPaths>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
