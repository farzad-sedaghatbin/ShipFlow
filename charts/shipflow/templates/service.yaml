apiVersion: v1
kind: Service
metadata:
  name: {{ include "shipflow.fullname" . }}
  labels:
    {{- include "shipflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "shipflow.selectorLabels" . | nindent 4 }}
